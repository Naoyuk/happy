<div class="main posts-new">
    <div class="container">
        <%= form_tag "/posts/#{@post.id}/update", class: "form posts-new" do %>
            <textarea name="content"><%= @post.content %></textarea>
            <input type="submit" value="Post">
        <% end %>
    </div>
</div>